---
name: CalculatorTest
description: This is a sample YAML format feature file testing a calculator's function
tags:
  - tag1
    tag2
    tag3
testSetupSteps:
  - identifier: Given the calculator is powered on

scenarioSetupSteps:
  - identifier: When the all clear button is pressed
  - identifier: Then the calculator should display "0"

testScenarios:
  - name: Scenario1
    tags:
      - tags1
        tags2
        tags3
    scenarioExecutionSteps:
      - identifier: When the button "1" is pressed
      - identifier: And the button "+" is pressed
      - identifier: And the button "2" is pressed
      - identifier: And the button "=" is pressed
      - identifier: And the calculator should display "3"

  - name: Scenario2
    scenarioExecutionSteps:
      - identifier: When a binary operation is perfomed on the calculator
        testData:
          operand1: 100
          operation: "/"
          operand2: 200
      - identifier: And the calculator should display "0.5"

scenarioTearDownSteps:
  - identifier: Then dummy teardown step

testTearDownSteps:
  - identifier: Then the calculator is powered off
